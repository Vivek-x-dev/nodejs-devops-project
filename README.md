# nodeDevops-project
Developer → GitHub → Webhook → Jenkins →  Build Docker Image → Push to Docker Hub →  EC2 Server:     → Pull Image     → Run via docker-compose     → Nginx Reverse Proxy     → HTTPS (SSL)      Environments:     → Staging     → Production
